import Actions from './DialogActions';
import Container from './DialogContainer';
import Description from './DialogDescription';
import Title from './DialogTitle';

const AppDialog = {
  Title,
  Container,
  Description,
  Actions,
};

export default AppDialog;
